
const Details = (props) => {
    return (
            <div className="Details">
                this will be the Snake Dashboard for {props.snake.name}
            </div>
        );    
}

export default Details;

