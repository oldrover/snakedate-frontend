export const EventForm = (props) => {
    
    return (
        <div className="EventForm">
            test
            <button onClick={props.handleClose}>
                Close
            </button>
        </div>
    )
}